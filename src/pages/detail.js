import React from 'react';
import {useParams} from "react-router-dom";

function Detail() {
    const {instagramId} = useParams();
    return (
        <div>

        </div>
    );
}

export {Detail};